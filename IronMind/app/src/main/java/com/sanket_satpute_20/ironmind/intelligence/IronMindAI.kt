package com.sanket_satpute_20.ironmind.intelligence

/**
 * Core abstraction for IronMind AI capabilities.
 * The AI acts as a reasoning layer and must not automatically mutate domain state
 * without passing through the Decision Engine (unless explicitly permitted).
 */
interface IronMindAI {
}
